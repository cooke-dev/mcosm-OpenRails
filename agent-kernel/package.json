{
  "name": "openrails-agent-kernel",
  "version": "0.1.0",
  "private": true,
  "description": "GIWA-native OpenRails workspace, Path, Pact, Proof, BNH runtime, and Gaia kernel.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "sql",
    "README.md",
    "SECURITY.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/test/*.test.js",
    "check": "npm run build && npm run test",
    "start": "node dist/server.js",
    "prepack": "npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^5.4.5"
  }
}
